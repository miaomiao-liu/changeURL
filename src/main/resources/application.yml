spring:
  #freemarker:
   # settings:
    #  number_format: 0.##
  datasource:
    password: root
    username: root
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/change_url?useUnicode=true&characterEncoding=utf8&useSSL=false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

